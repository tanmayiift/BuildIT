# BuildIT audit status — 2026-09-14

## Closed with evidence

- The Convex metric summary and usage budget repairs are in the source patch, with focused regressions and the final verification passing.
- 45 actual Chrome cases passed against the local BuildIT services, including owner/viewer controls, workspace isolation, usage, metrics, keyboard navigation, sign-out, and a 400px model form.
- The duplicate `pulsetrade/backend-vercel` Git connection to `tanmayiift/BuildIT` was removed. Its project settings and existing deployment were preserved. The failure was reproduced as a missing root `dist` output directory after a successful web build.
- BuildIT Dependabot routine updates are grouped weekly with a low pull-request cap. Routine pnpm version updates are disabled; security coverage remains enabled. GitHub vulnerability alerts and automatic security fixes were enabled and read back for `tanmayiift/BuildIT`.
- The existing BuildIT Grafana email contact was tested successfully and the message was observed in Gmail. Live `buildit_snapshot` telemetry was observed at 2026-09-14T06:19:23.593Z, about three minutes before the 06:22:23 observation.
- All 14 current Grafana BuildIT rules were updated through the authenticated UI with exactly two approved fields per rule: execution errors now become `Error`, and the existing BuildIT email contact is explicit. Native export comparison found 28 approved changes and zero unintended or structural changes.
- Isolated local monitoring proved heartbeat firing/recovery and all 14 current-rule `DatasourceError` firing/recovery notifications. Disposable containers, network and volumes were removed.
- Telemetry now preserves `capacity_exhausted` from the shared SDK and durable worker; a regression prevents the runner-capacity alert from being collapsed into `other`.
- Final `pnpm verify`: 1,971 tests in 223 files; lint, type checks and production builds passed. Security release checks: 702 tests in 88 files with a fresh OSV database. Reliability release checks: 335 tests in 28 files. No OSV advisories were reported.
- The release dry run now passes with the normal pnpm path: web `apps/web`, broker `packages/broker`, and coordinated order `broker → convex → web`; it starts no deployment.
- Production read-only health is stale: web `/api/health` returns 404 and broker health returns 200 while serving baseline commit `e9dc368…`. The production Convex preflight also reports the repaired review index absent and September accounting reconciliation incomplete.
- The supported CUA browser matrix completed 45/45 named Chrome cases. The full shell Playwright run is separately recorded as an environment limitation: macOS denied Chromium's helper process before assertions (205 launch failures, 2 completed). The equivalent GitHub Actions Chromium job passed (`Build and test`, run `34811746547`, job `104005470869`), but that was not a production journey.

## Open or blocked

- The reviewed source patch is not deployed to production. The production Convex deployment remains unpaused and the separate Vercel CLI device authorization is unavailable. A production release needs a fresh valid deployment credential and immutable commit gate.
- The durable execution record, lease, replay, and failure checkpoint are implemented and tested locally. The actual broker request is still synchronous and the AWS ECS/Fargate + SQS worker migration is not deployed, so the 300-second production execution acceptance criterion is open.
- Vercel BuildIT Pro is active and has billed Sandbox and build usage. Cancellation is not confirmed safe: Hobby is personal/noncommercial and has a 300-second function ceiling versus the broker's 800-second execute route. Sandbox itself is available on Hobby with a shorter 45-minute session cap, but that does not solve the broker timeout. BuildIT needs an approved execution migration or continued Pro service before cancellation.
- Eight legacy Grafana rules have exact predicate matches and a reviewed backup; four have materially different failure predicates. The approved pause request was attempted, but Grafana exposes these provisioned rules without a pause-evaluation action. All remain active; no legacy rule was deleted or silenced. An API/service-account path is needed to apply the pause.
- Three open medium GitHub alerts concern the same Vitest advisory and remain until the fixed default-branch revision is published and GitHub rescans it.
- The authenticated Dependabot Vitest PR (`#54`) was rerun. Its browser job passed in CI, while both quality jobs exposed the Vitest 4 mock type mismatch fixed locally in `packages/broker/test/artifacts.test.ts`; the release-gates job also exposed the stale OSV cache path fixed locally with the metadata-API fallback. Those fixes are not published because this Desktop checkout cannot write its Git index and the elevated staging request was rejected by the host usage-limit review.
- The new AWS key policy is saved and verified. The authenticated S3 console now shows a fresh `DailyDeletionAudit` manifest (544 B), checksum (33 B), and compressed CSV (130 B) in the destination, all modified on 2026-09-14 after the repair and encrypted with the BuildIT KMS key. Direct object content was blocked by the in-app browser and the shell has no AWS CLI credentials, so parsing and the next scheduled run remain follow-up checks; receipt: `audit/evidence/aws-inventory-console-read-2026-09-14.json`.
- Live external model, Ask, Autofix, GitHub review publication, tracker OAuth and customer email journeys remain untested or unavailable. No paid model or sandbox call was made.
- The latest `BUILDIT_E2E_BASE_URL=http://127.0.0.1:3107 pnpm test:e2e` run started 207 tests but macOS terminated Chromium's helper before assertions (`bootstrap_check_in … Permission denied`); two HTTP-style cases passed. The supported CUA matrix remains 45/45. Full axe accessibility scan, exact historical screenshot comparison, large-dataset browser caps, real OAuth/provider/sandbox execution, and customer email transport remain untested.

Every open item is retained as open in `PLAN.md`; passing local checks do not imply production deployment or full external coverage.
