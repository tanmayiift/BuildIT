node_modules
.next
dist
coverage
playwright-report
test-results
.env*
!.env.example
.vercel
*.log
*.tsbuildinfo
.DS_Store
PLAN.md
.local/
docs/plan-BuildIT.md
.agents/
docs/release/deployment.md
.claude/launch.json

# Created by the pinned Playwright container when snapshots are regenerated on Linux.
.pnpm-store/

# Raw run transcripts from the audit harness. The receipts that get cited - the markdown summaries
# and the JSON manifests - are committed; these are the 2.6MB of stdout behind them, and a
# 1.3MB e2e server log in permanent history is bloat nobody will ever read.
audit/**/*.log
