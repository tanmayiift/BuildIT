# Disaster recovery

Restore encrypted control-plane state to the tested recovery point, rotate service credentials, verify audit-chain heads and tenant isolation, and re-enable triggers gradually. In-flight reviews are not restored; users may start linked retries.
