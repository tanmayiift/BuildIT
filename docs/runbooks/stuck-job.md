# Stuck job

Disable new work for the affected repository, inspect the last persisted stage and idempotency keys, terminate its sandbox, reconcile the GitHub Check against its pinned commit, then create a linked retry. Never reactivate a terminal record.
