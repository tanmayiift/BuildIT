# Provider outage

Enable the provider kill switch, leave queued work visible, pause active work as `blocked`, notify affected owners without source content, and resume only current pinned heads after health recovers.
