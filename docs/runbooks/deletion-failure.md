# Deletion failure

Alert security and the organization owner, retry the exact artifact key with bounded backoff, retain an audit record without content, and escalate after five failures. Do not report deletion complete until storage confirms absence.
