# BuildIT status — 16 September 2026

Measured, not inherited. Every line here was checked today.

## Does it work?

**Yes, except for one thing, and that thing is a spending limit rather than code.**

Proven in production, right now, with no account and no model key:

```
POST https://buildit-agentic-review.vercel.app/api/scan
→ buildit-tls-disabled · critical · server.js:1 · "TLS certificate verification is disabled"
  ran: buildit-rules, secret-patterns
  didNotRun: gitleaks, osv-scanner, tests, lint, typecheck, AI review
```

That is the whole product in miniature: a real finding on a real line, and an honest list of what it
did *not* check. A stranger can do that on the landing page today.

## What is green

| | |
| --- | --- |
| `pnpm verify` | 2044 tests / 227 files |
| security · reliability · eval · alerts · dashboard · deploy checks | all pass |
| GitHub Actions on `main` | **green** |
| Vercel deployments, last 6 | **all READY** |
| Web, broker, Convex | deployed and healthy |
| Grafana | 14 active rules, 12 legacy paused, dashboard intact |
| AWS inventory | manifest + checksum + CSV written post-repair |

## What production actually shows

40 most recent reviews, read from production today:

| Status | Count |
| --- | ---: |
| `platform_failed` | 25 |
| `changes_requested` | 7 |
| `checks_passed` | 5 |
| `cancelled` | 3 |

**Twelve of those are real verdicts** — `blocking_findings` and `checks_complete`, the last on
5 September. The product has reviewed pull requests and said something true about them. What
followed was not a regression in the review path; it was two external allowances running out, one
after the other:

| When | `statusReasonCode` | What it means |
| --- | --- | --- |
| 5 Sep | `checks_complete`, `blocking_findings` | working |
| 14 Sep | `provider_rate_limited` → `provider_quota_exhausted` | the model account ran dry |
| 16 Sep | `sandbox_unavailable` | the Hobby CPU allowance ran dry |

**The segmented 300-second execution is proven.** Of the execution jobs behind the 14 September
reviews, **seven reached `complete` on attempt 1** — `prepare` through `complete`, no re-entry, no
retry. Those runs failed afterwards, at the model call, on a provider with no credit. The part that
had to be rebuilt for Hobby did its job; the part that failed was a billing balance.

## The two things stopping a full PR review

**1. The provider fallback was off for the one failure that needed it.** Fixed today — see
`docs/operations/known-defects.md`. A valid Gemini credential has been connected since 31 August and
never once used, because `provider_quota_exhausted` was missing from the set of failures that start
a fallback review. It resolves on paper and is unproven in production, because of:

**2. Vercel Hobby's sandbox CPU allowance is spent for this billing cycle.**

| Sandbox quota | Used | Hobby limit |
| --- | ---: | ---: |
| Creations | 434 | 5,000 |
| **Active CPU** | **8h 12m** | **5h** |
| Provisioned memory | 49.0 GB-Hrs | 420 GB-Hrs |
| Data transfer | 5.68 GB | 20 GB |

Only CPU is over. On Pro that overage was billed on demand; Hobby stops, which surfaces as
`sandbox_unavailable` and, in the job record, `validation_runner_failed` at `prepare` on attempt 3.
**No review can run to completion until the cycle resets or the plan changes.**

Most of that 8h12m was spent by the *old* execution design, which paid sandbox setup on every single
review. The segmented design pays it once per job. It should use materially less — but that is a
prediction and it stays one until it can be measured.

## The decision, stated once

| Option | Cost | What you get |
| --- | --- | --- |
| **Wait for the cycle to reset** | £0 | Reviews resume, and we finally measure what a segmented review actually costs in CPU |
| **Return to Pro** | ~$20/mo | Reviews work today; overage is billable rather than blocking |

Nothing else is waiting on either choice. Everything that can be built has been.

## Corrections to earlier claims

Three things previously recorded here were wrong, and stayed wrong long enough to matter:

1. *"The web health endpoint returns 404"* was listed as a verified blocker. The route was
   **untracked** — it 404'd because 191 finished files had never been committed.
2. *"Grafana exposes no pause action for provisioned rules."* It does. The update was being sent
   with `x-disable-provenance`, which belongs on create and asks Grafana to rewrite provenance on
   update. Removing it paused 12 of 12.
3. *"Hobby's constraint is a 45-minute sandbox session cap."* It is not. It is 5 CPU-hours a month;
   session length never came into it.

## Still open, and honestly so

- **Model provider credit.** A key exists and reviews get past the credential check. OpenAI's key
  returns 429 `insufficient_quota` — BuildIT now says *"the account has no credit left, this is not
  a rate limit and will not clear on its own"* instead of telling you to wait.
- **AWS credentials in CI.** `smoke:aws-boundary` warns rather than failing, and
  `docs/operations/ci-gates.md` records exactly what goes unverified until the secrets exist.
- **Tracker OAuth and an email sending domain.** Code written and tested; no account to connect to.
- **`findingResolution: "fixed"`** — deliberately unwritten. Marking findings fixed on delivery would
  over-claim; see `docs/operations/known-defects.md`.
