import{fromWebToken}from"@aws-sdk/credential-providers";import{AwsKmsClient}from"@buildit/security";import{ConvexCredentialGateway}from"../src/convex-gateway.js";import{TrackerCredentialBroker}from"../src/tracker-credentials.js";import{handleTrackerCredentialSave}from"../src/tracker-credential-http.js";function required(name:string){const value=process.env[name];if(!value)throw new Error("broker_configuration_missing");return value}async function route(request:Request){const authorization=request.headers.get("authorization")??"",token=authorization.startsWith("Bearer ")?authorization.slice(7):"unauthenticated-request";try{const region="eu-west-1",oidc=request.headers.get("x-vercel-oidc-token")??process.env.VERCEL_OIDC_TOKEN;if(!oidc)throw new Error("broker_configuration_missing");const credentials=fromWebToken({roleArn:required("AWS_ROLE_ARN"),webIdentityToken:oidc,roleSessionName:`buildit-tracker-save-${Date.now()}`,clientConfig:{region}}),gateway=new ConvexCredentialGateway(required("CONVEX_URL"),token),broker=new TrackerCredentialBroker(gateway,new AwsKmsClient({config:{region,credentials}}),required("AWS_KMS_KEY_ID"));return handleTrackerCredentialSave(request,{allowedOrigin:required("BUILDIT_WEB_ORIGIN"),authorize:gateway.authorize,broker})}catch{return Response.json({error:"broker_unavailable"},{status:503,headers:{"cache-control":"no-store"}})}}export const POST=route;export const OPTIONS=route;
import { registerBrokerTelemetry } from "../src/instrumentation.js";

registerBrokerTelemetry();
