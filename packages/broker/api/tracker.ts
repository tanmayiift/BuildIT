import{S3Client}from"@aws-sdk/client-s3";import{fromWebToken}from"@aws-sdk/credential-providers";import{AwsKmsClient}from"@buildit/security";import{S3GrantConsumer}from"../src/artifacts.js";import{handleTrackerFetch}from"../src/tracker-http.js";
function required(name:string){const value=process.env[name];if(!value)throw new Error("tracker_broker_configuration_missing");return value}export const POST=async(request:Request)=>{try{const region="eu-west-1",oidcToken=request.headers.get("x-vercel-oidc-token")??process.env.VERCEL_OIDC_TOKEN;if(!oidcToken)throw new Error("tracker_broker_configuration_missing");const credentials=fromWebToken({roleArn:required("AWS_ROLE_ARN"),webIdentityToken:oidcToken,roleSessionName:`buildit-tracker-${Date.now()}`,clientConfig:{region}}),bucket=required("AWS_ARTIFACT_BUCKET"),kmsKeyId=required("AWS_KMS_KEY_ID"),s3=new S3Client({region,credentials}),replay=new S3GrantConsumer({bucket,kmsKeyId,s3:s3 as never}),kms=new AwsKmsClient({config:{region,credentials}});return handleTrackerFetch(request,{grantSecret:Buffer.from(required("TRACKER_GRANT_SECRET"),"base64url"),consume:(id,expiresAt)=>replay.consume(id,expiresAt),kms})}catch{return Response.json({error:"tracker_broker_unavailable"},{status:503,headers:{"cache-control":"no-store"}})}};
