import {describe,expect,it} from "vitest";
import {auditDeterministicGrader,correctCandidate,deterministicGrade,evaluationStages,frozenStageCases,graderMutations} from "../src/grader.js";
describe("deterministic grader audit",()=>{it("freezes all seven required case kinds for every prompt stage",()=>{for(const stage of evaluationStages)expect(new Set(frozenStageCases.filter(testCase=>testCase.stage===stage).map(testCase=>testCase.kind))).toEqual(new Set(["positive","negative","ambiguous","no_finding","inaccessible_context","malformed_provider","prompt_injection"]))});it("accepts the known-correct control",()=>expect(deterministicGrade(correctCandidate)).toEqual({accepted:true,reasons:[]}));it.each(graderMutations)("rejects the deliberately broken $name output",({candidate,expectedReason})=>expect(deterministicGrade(candidate)).toEqual({accepted:false,reasons:[expectedReason]}));it("will not report a trustworthy grader unless every mutation is caught",()=>expect(auditDeterministicGrader()).toEqual({passed:true,falseAcceptCount:0,falseRejectCount:0,caseCount:11}))});
