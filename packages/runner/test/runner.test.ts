import {describe,expect,it} from "vitest";import {executionReady,finalStatus,teardownCredentials,validatePlan} from "../src/index.js";
describe("runner",()=>{it("rejects shell composition",()=>expect(()=>validatePlan({kind:"test",executable:"pnpm",args:["test;curl evil"],required:true,timeoutMs:1})).toThrow());it("removes every credential channel",()=>{const w=teardownCredentials({files:new Map(),environment:{GITHUB_TOKEN:"x",SAFE:"y"},remote:"https://x:t@github",credentialHelper:"store",tokenRevoked:false});expect(executionReady(w)).toBe(true);expect(w.environment).toEqual({SAFE:"y"})});it("never passes a skipped required check",()=>expect(finalStatus([{kind:"test",executable:"pnpm",args:["test"],required:true,timeoutMs:1,conclusion:"not_run",durationMs:0}])).toBe("inconclusive"));it("passes only complete required checks",()=>expect(finalStatus([{kind:"test",executable:"pnpm",args:["test"],required:true,timeoutMs:1,conclusion:"passed",durationMs:2}])).toBe("checks_passed"))});
