import {describe,expect,it} from "vitest";import {decryptSecret,encryptSecret,fingerprint,redact,sanitizeGitHub} from "../src/index.js";import {randomBytes} from "node:crypto";
describe("security",()=>{it("binds ciphertext to its organization",()=>{const k=randomBytes(32),c=encryptSecret("sk-ant-secretvalue",k,"org:a");expect(decryptSecret(c,k,"org:a")).toBe("sk-ant-secretvalue");expect(()=>decryptSecret(c,k,"org:b")).toThrow()});it("redacts and neutralizes output",()=>{expect(sanitizeGitHub("@buildit use ghp_abcdefghijk <img src=x>")).toBe("＠buildit use [REDACTED] ")});it("uses keyed stable fingerprints",()=>{const k=randomBytes(32);expect(fingerprint("x",k)).toBe(fingerprint("x",k));expect(fingerprint("x",k)).not.toBe(fingerprint("y",k))});it("redacts provider keys",()=>expect(redact("token sk-proj_abcdefghijk")).not.toContain("abcdefghijk"))});
